apiVersion: apps/v1
kind: Deployment
metadata:
  name: ranking-service
  namespace: news-platform
spec:
  replicas: 4
  selector:
    matchLabels:
      app: ranking-service
  template:
    metadata:
      labels:
        app: ranking-service
    spec:
      containers:
        - name: ranking-service
          image: ghcr.io/example/news/ranking-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: news-platform-config
---
apiVersion: v1
kind: Service
metadata:
  name: ranking-service
  namespace: news-platform
spec:
  selector:
    app: ranking-service
  ports:
    - port: 3000
      targetPort: 3000

