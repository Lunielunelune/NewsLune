apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: news-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: ghcr.io/example/news/notification-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: news-platform-config
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: news-platform
spec:
  selector:
    app: notification-service
  ports:
    - port: 3000
      targetPort: 3000

