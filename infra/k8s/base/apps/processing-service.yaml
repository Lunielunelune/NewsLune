apiVersion: apps/v1
kind: Deployment
metadata:
  name: processing-service
  namespace: news-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app: processing-service
  template:
    metadata:
      labels:
        app: processing-service
    spec:
      containers:
        - name: processing-service
          image: ghcr.io/example/news/processing-service:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: news-platform-config
---
apiVersion: v1
kind: Service
metadata:
  name: processing-service
  namespace: news-platform
spec:
  selector:
    app: processing-service
  ports:
    - port: 3000
      targetPort: 3000

